#!/usr/bin/env python3
"""Roadbook JSON -> responsive AMap JS API 2.0 roadbook.

The output is a single-file app with an overview and hash-addressable daily detail
pages. It uses road geometry returned by the route provider when available. Rough
routes without geometry are calculated in the browser with AMap.Driving and are
clearly marked as needing motorcycle/highway verification.
"""

import argparse
import html
import json
import os
import sys

from roadbook_utils import estimate_fuel, load_roadbook, to_gcj02


def convert_coordinates(rb):
    source = rb["coordinate_system"]

    def convert_array(point):
        lng, lat = to_gcj02(point[0], point[1], source)
        return [lng, lat]

    def convert_object(point):
        if isinstance(point, dict) and isinstance(point.get("lng"), (int, float)) and isinstance(point.get("lat"), (int, float)):
            point["lng"], point["lat"] = to_gcj02(point["lng"], point["lat"], source)

    for day in rb["days"]:
        day["start_coords"] = convert_array(day["start_coords"])
        day["end_coords"] = convert_array(day["end_coords"])
        if day.get("route_geometry"):
            day["route_geometry"] = [convert_array(point) for point in day["route_geometry"]]
        for route in day.get("alternative_routes", []):
            if route.get("route_geometry"):
                route["route_geometry"] = [convert_array(point) for point in route["route_geometry"]]
        for collection in ("waypoints", "fuel_stops", "scenic_spots", "bailout_points", "context_pois", "lodging_options"):
            for point in day.get(collection, []):
                convert_object(point)
        for scenic_route in day.get("scenic_routes", []):
            if scenic_route.get("route_geometry"):
                scenic_route["route_geometry"] = [convert_array(point) for point in scenic_route["route_geometry"]]
        convert_object(day.get("lodging"))
    rb["coordinate_system"] = "gcj02"


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("roadbook")
    parser.add_argument("-o", "--output", default=None)
    parser.add_argument("--amap-key", default=os.environ.get("AMAP_JS_KEY", ""), help="高德 Web端(JS API) Key；也可用 AMAP_JS_KEY")
    parser.add_argument("--amap-security-code", default=os.environ.get("AMAP_JS_SECURITY_CODE", ""), help="高德 JS API 安全密钥；也可用 AMAP_JS_SECURITY_CODE")
    args = parser.parse_args()
    try:
        rb = load_roadbook(args.roadbook)
    except (OSError, ValueError) as exc:
        sys.exit(str(exc))

    convert_coordinates(rb)
    consumption = rb.get("rider", {}).get("consumption_l_per_100km", 3.5)
    for day in rb["days"]:
        day["estimated_fuel_l"] = 0 if day.get("is_gap_day") else round(estimate_fuel(day, consumption), 1)

    out = args.output or "roadbook.html"
    days_js = json.dumps(rb["days"], ensure_ascii=False).replace("</", "<\\/")
    crowd_js = json.dumps(rb.get("crowd_avoidance", {}), ensure_ascii=False).replace("</", "<\\/")
    config_js = json.dumps({"key": args.amap_key, "securityCode": args.amap_security_code}, ensure_ascii=False).replace("</", "<\\/")
    safe_title = html.escape(rb.get("title", "摩旅路书"), quote=True)

    template = r'''<!DOCTYPE html>
<html lang="zh-CN"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="color-scheme" content="light">
<title>__TITLE__</title>
<style>
:root{--asphalt:#16252d;--slate:#30444d;--paper:#f7f8f4;--outbound:#ec6a38;--return:#247985;--local:#c89438;--alternate:#6aaec4;--warning:#c7493a;--line:#d7dedb;--muted:#64757c;--white:#fff;--shadow:0 18px 45px rgba(18,35,43,.16)}
*{box-sizing:border-box}html,body{height:100%;margin:0}body{overflow:hidden;background:var(--paper);color:var(--asphalt);font-family:"PingFang SC","Microsoft YaHei",system-ui,sans-serif}button,input{font:inherit}button{cursor:pointer}.shell{display:grid;grid-template-columns:360px 1fr;height:100%}.rail{position:relative;z-index:5;display:flex;min-height:0;flex-direction:column;background:var(--paper);box-shadow:8px 0 35px rgba(18,35,43,.14)}
.mast{padding:28px 26px 20px;background:var(--asphalt);color:#fff}.eyebrow{font:700 11px/1 "DIN Alternate","Avenir Next",sans-serif;letter-spacing:.2em;text-transform:uppercase;color:#a9c0c8}.mast h1{margin:10px 0 12px;font-size:27px;line-height:1.2;letter-spacing:-.02em}.mast-meta{display:flex;gap:14px;color:#c8d4d8;font-size:12px}.route-list{overflow:auto;padding:14px 14px 96px}.day-card{width:100%;display:grid;grid-template-columns:48px 1fr auto;gap:10px;align-items:center;margin:0 0 8px;padding:12px;border:1px solid transparent;border-radius:12px;background:transparent;color:inherit;text-align:left;transition:.2s ease}.day-card:hover,.day-card:focus-visible{background:#fff;border-color:var(--line);outline:none}.day-card.active{background:#fff;border-color:#bdc9c5;box-shadow:0 8px 20px rgba(18,35,43,.08)}.day-no{font:800 17px/1 "DIN Alternate","Avenir Next",sans-serif;color:var(--outbound)}.day-route{min-width:0}.day-route strong,.day-route small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.day-route strong{font-size:14px}.day-route small{margin-top:4px;color:var(--muted);font-size:11px}.open-label{font-size:11px;color:var(--muted)}
.map-wrap{display:grid;grid-template-rows:auto minmax(0,1fr) auto;min-width:0;min-height:0}.weather-strip{display:grid;grid-template-columns:auto repeat(3,minmax(0,1fr));gap:18px;align-items:center;padding:11px 18px;border-bottom:1px solid var(--line);background:#fff}.weather-day{font:800 13px/1 "DIN Alternate","Avenir Next",sans-serif;color:var(--outbound)}.weather-cell span{display:block;color:var(--muted);font-size:9px;letter-spacing:.08em}.weather-cell strong{display:block;margin-top:3px;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.map-stage{position:relative;min-height:0}.map{height:100%;background:#dce5e3}.map-status{position:absolute;top:18px;left:18px;z-index:4;max-width:min(520px,calc(100% - 36px));padding:10px 13px;border-radius:9px;background:rgba(22,37,45,.88);color:#fff;font-size:12px;backdrop-filter:blur(8px)}.legend{display:flex;flex-wrap:wrap;align-items:center;gap:2px 14px;font-size:10px;color:#d9e3e5}.map-footer .swatch{margin:0}.swatch{display:inline-block;width:19px;height:4px;margin:0 5px 2px 10px;vertical-align:middle}.swatch:first-child{margin-left:0}.swatch.outbound{background:var(--outbound)}.swatch.return{background:var(--return)}.swatch.local{background:var(--local)}.swatch.alt{height:0;border-top:4px dashed var(--alternate)}.map-footer{display:flex;flex-wrap:wrap;justify-content:space-between;gap:8px 18px;padding:9px 16px;background:var(--asphalt);color:#d9e3e5;font-size:10px;line-height:1.45}.map-footer strong{color:#fff}.map-footer .disclaimer{color:#ffd4c3;font-weight:800}
.detail{position:absolute;z-index:8;top:18px;right:18px;bottom:18px;width:min(470px,calc(100% - 36px));overflow:auto;padding:0 24px 28px;border:1px solid rgba(255,255,255,.7);border-radius:18px;background:rgba(247,248,244,.96);box-shadow:var(--shadow);backdrop-filter:blur(14px);transform:translateX(calc(100% + 40px));transition:transform .28s ease}.detail.open{transform:none}.detail-head{position:sticky;top:0;z-index:2;margin:0 -24px 20px;padding:22px 24px 16px;background:rgba(247,248,244,.97);border-bottom:1px solid var(--line)}.detail-head-top{display:flex;justify-content:space-between;gap:16px}.detail h2{margin:5px 0 0;font-size:24px;line-height:1.2}.close{width:36px;height:36px;border:1px solid var(--line);border-radius:50%;background:#fff;color:var(--asphalt);font-size:19px}.route-hero{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:12px;padding:17px;border-radius:13px;background:var(--asphalt);color:#fff}.route-hero span{font-size:11px;color:#9db2ba}.route-hero strong{display:block;margin-top:3px}.route-arrow{color:var(--outbound);font-weight:900}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:12px 0 22px}.metric{padding:12px 10px;border:1px solid var(--line);border-radius:10px;background:#fff}.metric span{display:block;color:var(--muted);font-size:10px}.metric strong{display:block;margin-top:4px;font:800 17px/1.1 "DIN Alternate","Avenir Next",sans-serif}.section{padding:17px 0;border-top:1px solid var(--line)}.section h3{margin:0 0 9px;font-size:13px;letter-spacing:.04em}.section p{margin:0;color:var(--slate);font-size:13px;line-height:1.65}.chips{display:flex;flex-wrap:wrap;gap:7px}.chip{padding:6px 9px;border-radius:999px;background:#fff;border:1px solid var(--line);font-size:11px}.hotel-grid{display:grid;gap:8px}.hotel{padding:11px;border:1px solid var(--line);border-radius:10px;background:#fff}.hotel strong,.hotel small{display:block}.hotel small{margin-top:4px;color:var(--muted);line-height:1.45}.risk-high{color:var(--warning);font-weight:700}.risk-medium{color:#a7651c;font-weight:700}.snow{border-left:4px solid var(--alternate);padding-left:12px}.empty{color:var(--muted)!important}.key-dialog{position:fixed;inset:0;z-index:30;display:none;place-items:center;padding:20px;background:rgba(22,37,45,.72);backdrop-filter:blur(8px)}.key-dialog.open{display:grid}.key-box{width:min(480px,100%);padding:26px;border-radius:18px;background:var(--paper);box-shadow:var(--shadow)}.key-box h2{margin:0 0 8px}.key-box p{color:var(--muted);font-size:13px;line-height:1.6}.key-box label{display:block;margin-top:14px;font-size:12px;font-weight:700}.key-box input{width:100%;margin-top:6px;padding:11px 12px;border:1px solid #b9c5c1;border-radius:8px;background:#fff}.primary{width:100%;margin-top:18px;padding:12px;border:0;border-radius:9px;background:var(--outbound);color:#fff;font-weight:700}.key-error{min-height:18px;margin-top:8px;color:var(--warning);font-size:12px}.map-pin{width:28px;height:28px;display:grid;place-items:center;border:2px solid #fff;border-radius:50% 50% 50% 8px;transform:rotate(-45deg);background:var(--asphalt);box-shadow:0 3px 10px rgba(0,0,0,.28);color:#fff}.map-pin span{transform:rotate(45deg);font:800 10px/1 sans-serif}.poi-label{padding:4px 7px;border:1px solid rgba(255,255,255,.8);border-radius:7px;background:rgba(22,37,45,.9);box-shadow:0 2px 8px rgba(0,0,0,.2);color:#fff;font-size:10px;white-space:nowrap}
@media(max-width:760px){.shell{grid-template-columns:1fr;grid-template-rows:51% 49%}.rail{grid-row:2;box-shadow:0 -8px 30px rgba(18,35,43,.13)}.mast{padding:12px 16px 10px}.mast h1{margin:5px 0 7px;font-size:18px}.eyebrow{font-size:9px}.route-list{display:flex;overflow:auto;padding:9px 12px 18px;gap:8px}.day-card{flex:0 0 240px;margin:0}.map-wrap{grid-row:1}.weather-strip{grid-template-columns:auto repeat(3,minmax(92px,1fr));gap:11px;overflow:auto;padding:8px 10px}.weather-cell strong{font-size:10px}.map-footer{padding:6px 10px;font-size:9px}.detail{position:fixed;top:auto;left:0;right:0;bottom:0;width:100%;max-height:82%;border-radius:18px 18px 0 0;transform:translateY(105%)}.detail.open{transform:none}.legend{gap:2px 9px}.map-status{top:10px;left:10px;max-width:calc(100% - 20px)}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
</style></head><body>
<main class="shell"><aside class="rail"><header class="mast"><div class="eyebrow">Western Sichuan / Moto roadbook</div><h1>__TITLE__</h1><div class="mast-meta"><span id="trip-days"></span><span id="trip-distance"></span><span>风景公路优先</span></div></header><nav id="route-list" class="route-list" aria-label="每日路线"></nav></aside><section class="map-wrap"><header id="weather-strip" class="weather-strip" aria-label="当日天气"></header><div class="map-stage"><div id="map" class="map"></div><div id="map-status" class="map-status">正在准备高德地图…</div><article id="detail" class="detail" aria-live="polite"></article></div><footer class="map-footer"><span class="disclaimer">仅供参考，请量力而行</span><div class="legend" role="group" aria-label="图例"><span class="swatch outbound"></span>去程 <span class="swatch return"></span>回程 <span class="swatch local"></span>驻地环线 <span class="swatch alt"></span>备选路线</div><span id="route-source">主路线轨迹来源待载入</span><span>出发前复核天气、路况、禁限摩及临时管制</span></footer></section></main>
<div id="key-dialog" class="key-dialog" role="dialog" aria-modal="true" aria-labelledby="key-title"><div class="key-box"><h2 id="key-title">连接高德地图</h2><p>此路书使用高德地图 JS API 2.0。请输入“Web端(JS API)”Key 与安全密钥；仅保存在当前浏览器会话中。</p><label>Web 端 Key<input id="amap-key" autocomplete="off"></label><label>安全密钥<input id="amap-code" autocomplete="off"></label><button id="connect" class="primary">加载道路地图</button><div id="key-error" class="key-error"></div></div></div>
<script>
const days=__DAYS__;const crowdPolicy=__CROWD__;const embeddedConfig=__CONFIG__;const phaseColors={outbound:'#ec6a38',return:'#247985',local:'#c89438',gap:'#64757c'};const poiColors={'草原':'#4f8a4b','山峰/垭口':'#765b91','县城':'#30444d','餐饮':'#b66a2e','住宿':'#4f6fa8','加油':'#c7493a','维修':'#5d6266','医院':'#b43d4b'};let map=null;let overlays=[];let currentDay=1;
const $=s=>document.querySelector(s);const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const validPoint=p=>Array.isArray(p)&&p.length===2&&p.every(Number.isFinite);function riskClass(value){value=String(value||'');return value.startsWith('高')?'risk-high':value.startsWith('中')?'risk-medium':''}
function renderRail(){const total=days.reduce((n,d)=>n+(Number(d.distance_km)||0),0);$('#trip-days').textContent=`${days.length} 天`;$('#trip-distance').textContent=`${Math.round(total)} km`;$('#route-list').innerHTML=days.map(d=>`<button class="day-card ${d.day===currentDay?'active':''}" data-day="${d.day}" aria-label="查看第${d.day}天详情"><span class="day-no" style="color:${phaseColors[d.route_phase]||phaseColors.outbound}">D${d.day}</span><span class="day-route"><strong>${esc(d.is_gap_day?'机动缓冲日':`${d.start} → ${d.end}`)}</strong><small>${esc(d.date||'日期机动')} · ${esc(d.distance_km)} km</small></span><span class="open-label">详情 ›</span></button>`).join('');document.querySelectorAll('.day-card').forEach(b=>b.addEventListener('click',()=>openDay(Number(b.dataset.day))))}
function updateWeather(d){const forecast=d.weather_forecast||{};const history=d.historical_weather||{};const temp=Number.isFinite(forecast.low_c)&&Number.isFinite(forecast.high_c)?`${forecast.low_c}–${forecast.high_c}℃`:forecast.summary||'预报待更新';const precip=Number.isFinite(forecast.precipitation_probability_pct)?`降水 ${forecast.precipitation_probability_pct}%`:'降水概率待更新';const historyParts=[];if(Number.isFinite(history.precipitation_probability_pct))historyParts.push(`雨雪 ${history.precipitation_probability_pct}%`);if(Number.isFinite(history.snow_probability_pct))historyParts.push(`降雪 ${history.snow_probability_pct}%`);const historyText=historyParts.join(' · ')||history.probability_summary||'无法计算可靠概率';$('#weather-strip').innerHTML=`<div class="weather-day">D${esc(d.day)} WEATHER</div><div class="weather-cell"><span>天气 / 温度</span><strong>${esc(temp)} · ${esc(forecast.summary||'')}</strong></div><div class="weather-cell"><span>当日预报</span><strong>${esc(precip)} · ${esc(forecast.updated_at||'待更新')}</strong></div><div class="weather-cell"><span>历史同期概率</span><strong title="${esc(history.probability_summary||'')}；${esc(history.sample_description||'')}">${esc(historyText)}</strong></div>`}
function block(title,body,klass=''){return `<section class="section ${klass}"><h3>${title}</h3>${body}</section>`}
function detailHtml(d){
 if(d.is_gap_day)return `<header class="detail-head"><div class="detail-head-top"><div><div class="eyebrow">D${d.day} / GAP DAY</div><h2>机动缓冲日</h2></div><button class="close" aria-label="关闭详情">×</button></div></header>${block('如何使用',`<p>${esc(d.notes||'用于吸收天气、管制、疲劳或高原反应造成的延误。')}</p>`)}`;
 const scenic=(d.scenic_routes||[]).map(r=>`<span class="chip">${esc(r.name)}${r.distance_km?` · ${esc(r.distance_km)}km`:''}</span>`).join('');
 const fuels=(d.fuel_stops||[]).map(s=>`<span class="chip">⛽ ${esc(s.name)}${s.note?` · ${esc(s.note)}`:''}</span>`).join('');
 const alts=(d.alternative_routes||[]).map(r=>`<p><strong>${esc(r.name)}</strong>${r.distance_km?` · ${esc(r.distance_km)}km`:''}<br>${esc(r.trigger||r.note||'按当天路况启用')}</p>`).join('');
 const hotels=(d.lodging_options||[]).map(h=>`<div class="hotel"><strong>${esc(h.name)}</strong><small>${esc(h.price_range||'价格待复核')} · 海拔 ${esc(h.altitude_m??'待核验')}m · ${esc(h.status||'verify')}</small><small>供氧：${esc(h.oxygen||'待问')}　地暖：${esc(h.heating||'待问')}　停车：${esc(h.parking||'待问')}</small><small>${esc(h.source||'来源待核验')} · ${esc(h.checked_at||'')}</small></div>`).join('');
 const snow=d.snow_risk||{},meals=d.meals||{},crowd=d.crowd_avoidance||{},history=d.historical_weather||{};
 return `<header class="detail-head"><div class="detail-head-top"><div><div class="eyebrow">D${d.day} / ${esc(d.date)}</div><h2>${esc(d.start)} → ${esc(d.end)}</h2></div><button class="close" aria-label="关闭详情">×</button></div></header><div class="route-hero"><div><span>起点</span><strong>${esc(d.start)}</strong></div><div class="route-arrow">→</div><div><span>终点 / 住宿</span><strong>${esc(d.end)}</strong></div></div><div class="metrics"><div class="metric"><span>里程</span><strong>${esc(d.distance_km)} km</strong></div><div class="metric"><span>预估油耗</span><strong>${esc(d.estimated_fuel_l)} L</strong></div><div class="metric"><span>最高海拔</span><strong>${esc(d.max_elevation_m)} m</strong></div></div>${block('风景路线',scenic?`<div class="chips">${scenic}</div>`:'<p class="empty">尚未标出独立风景路段</p>')}${block('酒店 / 民宿建议（飞猪优先）',hotels?`<div class="hotel-grid">${hotels}</div>`:'<p class="empty">飞猪未连接或无可靠结果；不得据此判断无房</p>')}${block('加油点',fuels?`<div class="chips">${fuels}</div>`:'<p class="empty">没有可靠加油点，出发前必须补齐</p>')}${block('备选路线',alts||`<p>${esc(d.plan_b||'未提供')}</p>`)}${block('顺路吃饭',`<p>午餐：${esc(meals.lunch||'待选')}<br>晚餐：${esc(meals.dinner||'待选')}</p>`)}${block('历史天气与落雪风险',`<p>${esc(history.probability_summary||'概率待核验')}<br><span class="${riskClass(snow.level)}">落雪风险 ${esc(snow.level||'未知')}</span>${snow.basis?` · ${esc(snow.basis)}`:''}<br>${esc(snow.action||'出发前按最新预报复核')}</p>`,'snow')}${block('避开人群与车流',`<p>${crowd.enabled===false?'用户未启用本日错峰':esc(crowd.plan||crowd.reason||'按用户指定的地点与时段执行；未指定则不擅自删减路线')}</p>`)}${block('路况与装备',`<p><span class="${riskClass(d.road_closure_risk)}">${esc(d.road_closure_risk)}</span><br>${esc(d.clothing)}</p>`)}`
}
function openDay(dayNo){currentDay=dayNo;const d=days.find(x=>x.day===dayNo);if(!d)return;renderRail();updateWeather(d);const source=d.route_source||{};$('#route-source').innerHTML=d.route_geometry?`主路线来自已核验道路轨迹：<strong>${esc(source.provider||'来源待核验')}</strong> · ${esc(source.strategy||'策略待核验')} · ${esc(source.checked_at||'日期待核验')}`:'主路线轨迹尚未核验';$('#detail').innerHTML=detailHtml(d);$('#detail').classList.add('open');$('#detail .close').addEventListener('click',closeDetail);location.hash=`day-${dayNo}`;drawDay(d)}function closeDetail(){$('#detail').classList.remove('open');history.replaceState(null,'',location.pathname+location.search)}function clearMap(){if(map&&overlays.length)map.remove(overlays);overlays=[]}
function marker(AMap,point,label,color,title=''){if(!validPoint(point))return null;return new AMap.Marker({position:point,anchor:'bottom-center',title:title||label,content:`<div class="map-pin" style="background:${color}"><span>${esc(label)}</span></div>`})}function poiMarker(AMap,poi){if(!Number.isFinite(poi.lng)||!Number.isFinite(poi.lat))return null;const type=poi.type||'县城',color=poiColors[type]||'#30444d';return new AMap.Marker({position:[poi.lng,poi.lat],anchor:'bottom-center',title:poi.name,content:`<div class="poi-label" style="border-color:${color}">${esc(poi.name)} · ${esc(type)}</div>`})}function drawGeometry(AMap,path,color,dashed=false){if(!Array.isArray(path)||path.length<2)return null;return new AMap.Polyline({path,strokeColor:color,strokeWeight:6,strokeOpacity:.92,strokeStyle:dashed?'dashed':'solid',lineJoin:'round',showDir:true})}
function drawDay(d){if(!map)return;clearMap();const AMap=window.__AMap;if(d.is_gap_day){const m=marker(AMap,d.end_coords,'休','#64757c');if(m){overlays.push(m);map.add(m);map.setFitView(overlays)}return}const routeColor=phaseColors[d.route_phase]||phaseColors.outbound;const main=drawGeometry(AMap,d.route_geometry,routeColor);if(main){overlays.push(main);map.add(main);$('#map-status').textContent='点击或缩放地图查看道路名、县城与周边 POI'}else{drawAmapRoute(d)};(d.alternative_routes||[]).forEach(r=>{const line=drawGeometry(AMap,r.route_geometry,'#6aaec4',true);if(line){overlays.push(line);map.add(line)}});const s=marker(AMap,d.start_coords,'起','#16252d',d.start),e=marker(AMap,d.end_coords,'终',routeColor,d.end);[s,e].filter(Boolean).forEach(m=>{overlays.push(m);map.add(m)});[...(d.context_pois||[]),...(d.fuel_stops||[]).map(p=>({...p,type:'加油'}))].forEach(p=>{const m=poiMarker(AMap,p);if(m){overlays.push(m);map.add(m)}});if(overlays.length)map.setFitView(overlays,false,[70,70,70,70])}
function drawAmapRoute(d){const AMap=window.__AMap;$('#map-status').textContent='正在用高德道路网络临时算路；仍需复核禁摩与高速路段';$('#route-source').innerHTML='主路线：<strong>高德浏览器临时算路</strong> · 非已核验轨迹';const driving=new AMap.Driving({policy:AMap.DrivingPolicy.LEAST_FEE,ferry:1,extensions:'all'});const opts={waypoints:(d.waypoints||[]).map(p=>new AMap.LngLat(p.lng,p.lat))};driving.search(new AMap.LngLat(...d.start_coords),new AMap.LngLat(...d.end_coords),opts,(status,result)=>{if(status==='complete'&&result.routes?.length){const path=result.routes[0].steps.flatMap(step=>step.path);const line=drawGeometry(AMap,path,phaseColors[d.route_phase]||phaseColors.outbound,true);if(line){overlays.push(line);map.add(line);map.setFitView(overlays)}}else{$('#map-status').textContent='临时算路失败：请补充 route_geometry 或检查高德 Key'}})}
function loadLoader(){return new Promise((resolve,reject)=>{if(window.AMapLoader)return resolve();const s=document.createElement('script');s.src='https://webapi.amap.com/loader.js';s.onload=resolve;s.onerror=()=>reject(new Error('无法加载高德 JS API Loader'));document.head.appendChild(s)})}async function connect(config){try{$('#key-error').textContent='';window._AMapSecurityConfig={securityJsCode:config.securityCode};await loadLoader();const AMap=await AMapLoader.load({key:config.key,version:'2.0',plugins:['AMap.Driving','AMap.ToolBar','AMap.Scale']});window.__AMap=AMap;map=new AMap.Map('map',{viewMode:'2D',zoom:7,center:[101.9,30.6],mapStyle:'amap://styles/normal',showLabel:true,features:['bg','point','road','building'],isHotspot:true});map.addControl(new AMap.ToolBar({position:{right:'18px',top:'70px'}}));map.addControl(new AMap.Scale());$('#key-dialog').classList.remove('open');sessionStorage.setItem('amap-roadbook-config',JSON.stringify(config));openDay(currentDay)}catch(err){$('#key-dialog').classList.add('open');$('#key-error').textContent=`加载失败：${err.message||err}`;$('#map-status').textContent='高德地图尚未连接'}}
$('#connect').addEventListener('click',()=>{const config={key:$('#amap-key').value.trim(),securityCode:$('#amap-code').value.trim()};if(!config.key||!config.securityCode){$('#key-error').textContent='Key 和安全密钥都需要填写';return}connect(config)});const hashDay=Number((location.hash.match(/day-(\d+)/)||[])[1]);currentDay=hashDay||days[0].day;openDay(currentDay);const saved=JSON.parse(sessionStorage.getItem('amap-roadbook-config')||'null');const initial=embeddedConfig.key&&embeddedConfig.securityCode?embeddedConfig:saved;if(initial)connect(initial);else{$('#key-dialog').classList.add('open');$('#map-status').textContent='输入高德 Web 端 Key 后显示道路地图'}
</script></body></html>'''
    page = template.replace("__TITLE__", safe_title).replace("__DAYS__", days_js).replace("__CROWD__", crowd_js).replace("__CONFIG__", config_js)
    with open(out, "w", encoding="utf-8") as handle:
        handle.write(page)
    key_note = "已内置高德 JS 配置" if args.amap_key and args.amap_security_code else "打开页面后输入高德 Web 端 Key"
    print(f"已导出 {out} ({len(rb['days'])} 天，{key_note})")


if __name__ == "__main__":
    main()
